{
  "name": "cotext-mcp",
  "version": "0.2.0",
  "description": "Cotext MCP Server — expose your Cotext context pool (and Neural Link graph) to AI agents (local + remote)",
  "type": "module",
  "bin": {
    "cotext-mcp": "dist/index.js"
  },
  "main": "dist/index.js",
  "files": ["dist", "README.md"],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js"
  },
  "keywords": ["cotext", "mcp", "context", "llm", "ai-agent", "neural-link", "knowledge-graph"],
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "glob": "^11.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.8.0"
  }
}
